/* Uses flag where counter expected and vice versa */
let flag inside_bold
let counter section_counter

bad_emit = {
    inc inside_bold
    ANY
}

bad_guard = {
    guard section_counter > 0
    with inside_bold { ANY }
}
